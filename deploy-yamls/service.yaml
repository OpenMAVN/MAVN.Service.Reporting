apiVersion: v1
kind: Service
metadata:
  labels:
    app: reporting
    environment: Development
  name: reporting
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: reporting
  sessionAffinity: None
  type: ClusterIP
